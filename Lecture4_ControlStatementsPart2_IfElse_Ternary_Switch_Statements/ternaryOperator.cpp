#include "iostream"
using namespace std;
int main(){
    // Ternary Operator =====>>>> Cool Banne Ke Ninja Technique.
    // Ternary Operator Is Nothing But A Cool And Short Form Of If-Else Statement.
    // Not That Important Thou.
    // Syntax ==>> condition ? if true     : if false
    //            statement1 ? statement 2 : statement 3
    int x;
    cout<<"Enter The Number :-\n";
    cin>>x;
    (x%2 == 0) ? (cout<<"The Number Entered Is Even\n") : (cout<<"The Number Entered Is Odd\n");
}